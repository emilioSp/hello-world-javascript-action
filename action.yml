name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  message:  # id of input
    description: 'Message'
    required: true
    default: 'Ciao!'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node12'
  main: 'index.js'
